spack:
  specs:
  - gcc +nvptx
  view: true
  concretizer:
    unify: true
    targets:
      granularity: generic
  config:
    install_tree:
      root: /opt/spack
      padded_length: 128
  mirrors:
    local-buildcache: oci://ghcr.io/psivaram/spack-buildcache
