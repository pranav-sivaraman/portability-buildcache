spack:
  specs:
  - gcc +nvptx ^cuda@=12.4.0
  view: true
  concretizer:
    unify: true
    targets:
      granularity: generic
  config:
    install_tree:
      root: /opt/spack
      padded_length: 128
  mirrors:
    develop: https://binaries.spack.io/develop
    local-buildcache: oci://ghcr.io/pranav-sivaraman/spack-buildcache

