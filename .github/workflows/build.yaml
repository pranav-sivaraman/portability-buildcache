name: "Perf-Port Spack Buildcache"
on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:
jobs:
  setup-buildcache:
    runs-on: ubuntu-latest
    permissions:
      packages: write
    container:
      image: quay.io/pypa/manylinux2014_x86_64
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup newer python
        run: |
          echo "SPACK_PYTHON=/opt/python/cp312-cp312/bin/python" >> $GITHUB_ENV

      - name: Setup Spack # Use older checkout for Spack
        uses: ./.github/actions/
        with:
          ref: develop
          buildcache: false

      - name: Trust buildcache
        run: |
          spack mirror add develop-2024-06-02 https://binaries.spack.io/develop-2024-06-02
          spack buildcache keys --install --trust

      - name: Find compilers
        run: spack compiler find --mixed-toolchain

      - name: Install newer gmake
        shell: spack-bash {0}
        run: |
          spack install gmake && spack load gmake

      - name: Concretize
        run: spack -e . -v concretize

      - name: Install
        run: |
          spack -e . env depfile -o Makefile
          make -Orecurse -j $(($(nproc) + 1)) SPACK_INSTALL_FLAGS=--no-check-signature

      - name: Push packages and update index
        run: |
          spack -e . mirror set --push --oci-username ${{ github.actor }} --oci-password "${{ secrets.GITHUB_TOKEN }}" local-buildcache
          spack -e . buildcache push -j $(($(nproc) + 1)) --base-image quay.io/pypa/manylinux2014_x86_64 --update-index --force local-buildcache
        if: ${{ !cancelled() }}
